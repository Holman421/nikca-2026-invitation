Temporary asset drop zone

Put final production assets here before replacing files in /public.

Expected final files currently used by the app:
- /public/flashlight.png  (favicon + app icon)
- /public/thumbnail.jpg   (Open Graph + Twitter preview image, ideally 1200x630)

Notes:
- Keep exact filenames above unless you also update app/layout.tsx metadata.
- You can keep working drafts in this folder and only copy finalized assets to /public.
